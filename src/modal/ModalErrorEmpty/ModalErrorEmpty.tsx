import { observer } from 'mobx-react-lite'
import React, {FC} from 'react'

const ModalErrorEmpty:FC = () => {
  return (
    <div>ModalErrorEmpty</div>
  )
}

export default observer(ModalErrorEmpty)